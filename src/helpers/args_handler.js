export class Args_Regex_Handler {
    static sp_url_handler (sp_url) {
        
    }
    
    static auth_email_handler (auth_email) {
        
    }

    static auth_password_handler (auth_password) {
        
    }
}